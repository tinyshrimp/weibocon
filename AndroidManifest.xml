<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="weibocon.org"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".UI.WeiboHome"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.WeiboDetail"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:launchMode="singleTask">
        </activity>
        <activity android:name=".UI.WeiboWrite"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:windowSoftInputMode="adjustResize"
                  android:launchMode="singleTask">
        </activity>
        <activity android:name=".UI.WeiboForward"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:windowSoftInputMode="adjustResize"
                  android:launchMode="singleTask">
        </activity>
        <activity android:name=".UI.WeiboReplyDisplay"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:launchMode="singleTask">
        </activity>
        <activity android:name=".UI.WeiboReplyEdit"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:windowSoftInputMode="adjustResize"
                  android:launchMode="singleTask">"
        </activity>
    </application>

</manifest>